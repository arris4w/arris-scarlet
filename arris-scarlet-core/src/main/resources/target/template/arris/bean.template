package ${common.package}.model;
${import}
/**
 * ${bean.comment}。
 *
 * @author ${common.user}
 * @version V1.0 ${common.date}
 */
public class ${bean.name} {
    ${fields}
    /**
     * 构造函数。
     */
    public ${bean.name}() {

    }

    /**
     * 构造函数。
     *
     ${construct.comment}
     */
    public ${bean.name}(${construct.param}) {
        ${construct.content}
    }
    ${getsets}
}
