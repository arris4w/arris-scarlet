
    /**
     * 取得${field.name}。
     *
     * @return 返回${field.name}。
     */
    public ${field.type} get${field.name.warp}() {
        return ${field.name};
    }

    /**
     * 设置${field.name}。
     *
     * @param ${field.name} 要设置的${field.name}。
     */
    public void set${field.name.warp}(${field.type} ${field.name}) {
        this.${field.name} = ${field.name};
    }
